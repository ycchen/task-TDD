<h1>Tasks</h1>


<%= render 'form' %>

<ul>
<% @tasks.each do |task| %>
	<li><%= task.task %> | <%= link_to "Edit", edit_task_path(task) %> | <%= link_to 'Destroy', task, :confirm => 'Are you sure?', :method => :delete %></li>
<% end %>
</ul>